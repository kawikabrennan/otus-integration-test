# otus-integration-test

# Requirements
* Install with Python 3.x 'pip install jsonschema'
* There are two test files. Run tests with:
  * 'python -m unittest test_search_response.py'
  * 'python -m unittest test_student_classes.py'
